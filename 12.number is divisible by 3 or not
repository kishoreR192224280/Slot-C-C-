#include <iostream>

int main() {
    int num;
    std::cout << "Enter an integer: ";
    std::cin >> num;

    std::string result = (num % 3 == 0) ? "The number is divisible by 3." : "The number is not divisible by 3.";

    std::cout << result << std::endl;

    return 0;
}
