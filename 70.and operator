#include <iostream>

int main() {
    int num;

    std::cout << "Enter an integer: ";
    std::cin >> num;

    if (num & 1) {
        std::cout << num << " is an odd number." << std::endl;
    } else {
        std::cout << num << " is not an odd number." << std::endl;
    }

    return 0;
}
